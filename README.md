# DETECTING AN INSIDER LLM 

This resarch invastigates a technique to see will we able to detect a LLM starts to missaligned with the obnjectives via specilized tool call for issues. 

The main argument is to see will the LLM will not LOG if they make a move to log their action. Normally an action should be loged authomatically. But issue is we will make a double loging. one will authomatic. Other will be observing will model will log the behaviour he did. LLM will think its loged only that spesific function call. 

The non-alignment between LLM loger and logging system itself will show the posibility of hallucination or INSIDER LLM.